package com.example.cards.configs;



class LoggableDispatcherServletTest {
  /*  @Test
    public void testDoDispatch() throws Exception {
        LoggableDispatcherServlet servlet = new LoggableDispatcherServlet();
        servlet.init();

        HttpServletRequest request = new MockHttpServletRequest();
        HttpServletResponse response = new MockHttpServletResponse();
        HandlerExecutionChain handler = Mockito.mock(HandlerExecutionChain.class);

        servlet.doDispatch(request, response);
        NavigationController  controller = new NavigationController();
        //TODO: Add assertions and fix
        //verify(handler).execute(any(HttpServletRequest.class), any(HttpServletResponse.class));
        // Add additional assertions as needed
    }*/

}
