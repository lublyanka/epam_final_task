<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection properties -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/mydatabase</property>
        <property name="hibernate.connection.username">myusername</property>
        <property name="hibernate.connection.password">mypassword</property>

        <!-- Hibernate dialect for generating SQL queries -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Automatically create tables and sequences -->
        <property name="hibernate.hbm2ddl.auto">create</property>

        <!-- Show SQL queries in console -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Mapping classes -->
        <mapping class="com.example.demo.model.User"/>
        <mapping class="com.example.demo.model.UserAddress"/>
        <mapping class="com.example.demo.model.UserDocument"/>
    </session-factory>
</hibernate-configuration>